# ============================================
# EMS Platform - CI/CD Pipeline with SonarQube
# ============================================

stages:
  - setup
  - quality
  - security
  - test
  - analysis
  - build
  - image_scan
  - infrastructure
  - deploy
  - finalize

variables:
  GIT_CLEAN_FLAGS: -ffdx -e client/node_modules/ -e backend/node_modules/
  DOCKER_TLS_CERTDIR: ""
  DOCKER_BUILDKIT: "1"
  NODE_VERSION: "20"
  POSTGRES_VERSION: "15"
  NEXT_TELEMETRY_DISABLED: "1"
  NPM_CONFIG_PREFER_OFFLINE: "true"
  NPM_CONFIG_AUDIT: "false"
  FRONTEND_IMAGE: "amineelalami/ems-platform-frontend"
  BACKEND_IMAGE: "amineelalami/ems-platform-backend"
  DB_IMAGE: "amineelalami/ems-platform-db"
  COMMIT_TAG: "sha-${CI_COMMIT_SHORT_SHA}"
  LATEST_TAG: "latest"
  COVERAGE_THRESHOLD: "80"
  TEST_TIMEOUT: "10000"
  NODE_ENV: "ci"
  CI: "true"
  SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"
  GIT_DEPTH: "0"
  SONAR_HOST_URL: "http://localhost:9000"

# === TEMPLATES ===
.docker_template: &docker_template
  image: docker:24
  variables:
    DOCKER_TLS_CERTDIR: ""
    DOCKER_BUILDKIT: "1"
  before_script:
    - echo "🔧 Docker Environment Setup"
    - docker --version
    - docker info
    - echo "📦 Logging into Docker Hub..."
    - echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
  tags:
    - windows
    - docker
    - ems-platform

.node_template: &node_template
  image: node:${NODE_VERSION}
  before_script:
    - echo "🔧 Node.js Environment Setup"
    - node --version
    - npm --version
  tags:
    - windows
    - docker
    - ems-platform

.node_postgres_template: &node_postgres_template
  image: node:${NODE_VERSION}
  services:
    - name: postgres:${POSTGRES_VERSION}
      alias: postgres
      command:
        [
          "postgres",
          "-c",
          "shared_preload_libraries=pg_stat_statements",
          "-c",
          "max_connections=200",
          "-c",
          "log_statement=all",
        ]
  variables:
    POSTGRES_HOST: postgres
    POSTGRES_PORT: 5432
    POSTGRES_DB: "ems_platform"
    POSTGRES_USER: "postgres"
    POSTGRES_PASSWORD: "password"
    POSTGRES_HOST_AUTH_METHOD: "trust"
    DATABASE_URL: "postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}"
  before_script:
    - echo "🔧 Node.js + PostgreSQL Environment Setup"
    - node --version
    - npm --version
    - echo "📦 Installing dependencies for PostgreSQL connection testing..."
    - apt-get update && apt-get install -y netcat-openbsd postgresql-client
    - echo "🔄 Waiting for PostgreSQL to be ready..."
    - |
      counter=0
      until pg_isready -h postgres -p 5432 -U postgres || [ $counter -eq 30 ]; do
        echo "⏳ Waiting for PostgreSQL... ($counter/30)"
        sleep 2
        counter=$((counter + 1))
      done
  tags:
    - windows
    - docker
    - ems-platform

.alpine_template: &alpine_template
  image: alpine:latest
  before_script:
    - echo "🔧 Alpine Linux Setup"
    - apk add --no-cache openssh-client curl postgresql-client bc
  tags:
    - windows
    - docker
    - ems-platform

.sonarqube_template: &sonarqube_template
  image:
    name: sonarsource/sonar-scanner-cli:latest
    entrypoint: [""]
  variables:
    SONAR_USER_HOME: "${CI_PROJECT_DIR}/.sonar"
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - .sonar/cache
  before_script:
    - echo "🔧 SonarQube Scanner Setup"
    - sonar-scanner --version
    - echo "🔍 Vérification des variables SonarQube..."
    - echo "SONAR_HOST_URL:http://localhost:9000"
    - echo "SONAR_TOKEN success"
  tags:
    - windows
    - docker
    - ems-platform

.trivy_template: &trivy_template
  image: aquasec/trivy:latest
  before_script:
    - echo "🔧 Installing Trivy..."
    - apk add --no-cache curl
    - curl -sfL https://raw.githubusercontent.com/aquasecurity/trivy/main/contrib/install.sh | sh -s -- -b /usr/local/bin
    - echo "✅ Trivy installed:"
    - trivy --version
  tags:
    - windows
    - docker
    - ems-platform

# === SETUP STAGE ===
install_frontend_deps:
  stage: setup
  <<: *node_template
  script:
    - |
      echo "📦 Installing Frontend dependencies..."
      cd client
      echo "🔍 Project structure:"
      ls -la
      echo "🗑️ Cleaning npm cache..."
      npm cache clean --force
      echo "📥 Attempting npm installation..."
      npm install --no-audit --no-fund --loglevel=verbose
      echo "✅ Frontend dependencies installed successfully"
      echo "📊 Dependency size:"
      du -sh node_modules || echo "Could not calculate size"
  cache:
    key: "frontend-npm-${CI_COMMIT_REF_SLUG}"
    paths:
      - client/node_modules/
    policy: pull-push
  artifacts:
    paths:
      - client/node_modules/
    expire_in: 1 hour
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 20 minutes

install_backend_deps:
  stage: setup
  <<: *node_postgres_template
  script:
    - |
      echo "📦 Installing Backend dependencies..."
      cd backend
      echo "🔍 Project structure:"
      ls -la
      echo "🔍 Checking for existing node_modules..."
      if [ -d "node_modules" ]; then
        echo "🔄 node_modules found, using cache..."
        npm ci --prefer-offline --no-audit
        echo "✅ Backend dependencies updated from cache"
      else
        echo "📥 Full installation (no cache found)..."
        npm install --prefer-offline --no-audit --no-fund
        echo "✅ Backend dependencies installed"
      fi
      echo "🔍 Verifying Prisma installation..."
      npx prisma@6.18.0 --version
      echo "🗄️ Generating Prisma client..."
      npx prisma@6.18.0 generate --no-hints
      echo "📊 Dependency size:"
      du -sh node_modules || echo "Could not calculate size"
  cache:
    key: "backend-npm-${CI_COMMIT_REF_SLUG}"
    paths:
      - backend/node_modules/
      - backend/node_modules/.prisma/
      - backend/node_modules/@prisma/
    policy: pull-push
  artifacts:
    paths:
      - backend/node_modules/
      - backend/node_modules/.prisma/
      - backend/node_modules/@prisma/
    expire_in: 1 hour
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 20 minutes

# === QUALITY STAGE ===
eslint_frontend:
  stage: quality
  <<: *node_template
  dependencies:
    - install_frontend_deps
  script:
    - |
      echo "📝 Running ESLint on Frontend..."
      cd client
      mkdir -p artifacts
      npx eslint src/ --format json --output-file artifacts/eslint-frontend.json || true
      echo "✅ ESLint completed"
  artifacts:
    paths:
      - client/artifacts/eslint-frontend.json
    expire_in: 1 week
    when: always
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

eslint_backend:
  stage: quality
  <<: *node_template
  dependencies:
    - install_backend_deps
  script:
    - |
      echo "📝 Running ESLint on Backend..."
      cd backend
      mkdir -p artifacts
      npx eslint . --ext .js,.ts --format json --output-file artifacts/eslint-backend.json || true
      echo "✅ ESLint completed"
  artifacts:
    paths:
      - backend/artifacts/eslint-backend.json
    expire_in: 1 week
    when: always
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

prettier_check:
  stage: quality
  <<: *node_template
  dependencies:
    - install_frontend_deps
    - install_backend_deps
  script:
    - |
      echo "🎨 Running Prettier check..."
      echo "📁 Frontend formatting..."
      cd client && npx prettier --check src/ || true
      cd ..
      echo "📁 Backend formatting..."
      cd backend && npx prettier --check . || true
      echo "✅ Prettier check completed"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

# === SECURITY STAGE ===
audit_frontend:
  stage: security
  <<: *node_template
  dependencies:
    - install_frontend_deps
  script:
    - |
      echo "🔒 Frontend security audit..."
      cd client
      mkdir -p artifacts
      npm audit --audit-level high --production --json > artifacts/audit-frontend.json || true
      echo "✅ Frontend audit completed"
  artifacts:
    paths:
      - client/artifacts/audit-frontend.json
    expire_in: 1 week
    when: always
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

audit_backend:
  stage: security
  <<: *node_template
  dependencies:
    - install_backend_deps
  script:
    - |
      echo "🔒 Backend security audit..."
      cd backend
      mkdir -p artifacts
      npm audit --audit-level high --production --json > artifacts/audit-backend.json || true
      echo "✅ Backend audit completed"
  artifacts:
    paths:
      - backend/artifacts/audit-backend.json
    expire_in: 1 week
    when: always
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

validate_prisma_schema:
  stage: security
  <<: *node_postgres_template
  dependencies:
    - install_backend_deps
  script:
    - |
      echo "🗄️ Prisma schema validation..."
      cd backend
      echo "🔍 Validating schema syntax..."
      npx prisma@6.18.0 validate
      echo "🔍 Testing database connection..."
      npx prisma@6.18.0 db execute --stdin --url "$DATABASE_URL" <<< "SELECT version();"
      echo "✅ Database connection successful"
      echo "✅ Prisma schema validated successfully"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 15 minutes
  allow_failure: false

trivy_filesystem_scan:
  stage: security
  <<: *trivy_template
  script:
    - |
      echo "🔍 Trivy Filesystem Security Scan..."
      mkdir -p artifacts
      trivy filesystem . --format json --output artifacts/trivy-fs-scan.json || true
      trivy filesystem . --format table --output artifacts/trivy-fs-scan.txt || true
      echo "✅ Trivy filesystem scan completed"
  artifacts:
    paths:
      - artifacts/trivy-fs-scan.json
      - artifacts/trivy-fs-scan.txt
    expire_in: 1 week
    when: always
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

# === TEST STAGE ===
test_frontend:
  stage: test
  <<: *node_template
  dependencies:
    - install_frontend_deps
  script:
    - |
      echo "🧪 Running Frontend tests with coverage..."
      cd client
      mkdir -p test-results
      mkdir -p coverage

      echo "📊 Running tests with coverage..."
      ./node_modules/.bin/jest --runInBand --watchAll=false --passWithNoTests --silent \
        --coverage --coverageDirectory=coverage \
        --collectCoverageFrom="[\"src/**/*.{js,jsx,ts,tsx}\",\"!src/**/*.d.ts\"]" \
        --coverageReporters=cobertura --coverageReporters=html --coverageReporters=text --coverageReporters=lcov

      echo "📈 Test Coverage Summary:"
      if [ -f "coverage/coverage-summary.json" ]; then
        cat coverage/coverage-summary.json | grep -A 4 '"total"'
      fi

      echo "✅ Frontend tests completed"
  artifacts:
    paths:
      - client/coverage/
      - client/test-results/
    reports:
      junit: client/test-results/*.xml
    expire_in: 1 week
    when: always
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: false
  timeout: 15 minutes

test_backend:
  stage: test
  <<: *node_postgres_template
  dependencies:
    - install_backend_deps
  script:
    - |
      echo "🧪 Running Backend tests..."
      cd backend
      mkdir -p coverage
      npm run test:ci
      echo "✅ Backend tests completed"
  artifacts:
    paths:
      - backend/coverage/
    reports:
      junit: backend/test-results/*.xml
    expire_in: 1 week
    when: always
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true
  timeout: 10 minutes

# === ANALYSIS STAGE (SONARQUBE) ===
sonarqube_setup:
  stage: analysis
  image: alpine:latest
  script:
    - |
      echo "🔧 Configuration SonarQube"
      echo "📋 Pour configurer SonarQube correctement:"
      echo ""
      echo "1️⃣  Créez un projet dans SonarQube:"
      echo "    - Allez sur: http://localhost:9000"
      echo "    - Cliquez sur 'Create project'"
      echo "    - Nom: 'ems-platform-frontend'"
      echo "    - Key: 'ems-platform-frontend'"
      echo "    - Répétez pour 'ems-platform-backend'"
      echo ""
      echo "2️⃣  Générer un token SonarQube:"
      echo "    - Connectez-vous à SonarQube"
      echo "    - Cliquez sur votre avatar → 'My Account'"
      echo "    - Allez dans 'Security'"
      echo "    - Sous 'Generate Tokens', nommez-le 'gitlab-ci'"
      echo "    - Copiez le token généré"
      echo ""
      echo "3️⃣  Configurez les variables GitLab CI/CD:"
      echo "    - Allez dans GitLab → Settings → CI/CD → Variables"
      echo "    - Ajoutez:"
      echo "      - SONAR_HOST_URL: http://localhost:9000"
      echo "      - SONAR_TOKEN: [votre_token_copié]"
      echo ""
      echo "🔍 Variables actuelles:"
      echo "SONAR_HOST_URL: ${SONAR_HOST_URL:-❌ NON DÉFINI}"
      echo "SONAR_TOKEN: $(if [ -n \"$SONAR_TOKEN\" ]; then echo '✅ DÉFINI'; else echo '❌ NON DÉFINI'; fi)"
      echo ""
      echo "📊 Prêt pour l'analyse SonarQube!"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
      when: always

sonarqube_frontend:
  stage: analysis
  <<: *sonarqube_template
  dependencies:
    - install_frontend_deps
    - test_frontend
  script:
    - |
      echo "📊 Analyse SonarQube - Frontend"

      # Vérification des variables obligatoires
      if [ -z "$SONAR_HOST_URL" ]; then
        echo "❌ SONAR_HOST_URL non défini"
        echo "📋 Configurez la variable CI/CD:"
        echo "   Nom: SONAR_HOST_URL"
        echo "   Valeur: http://localhost:9000"
        exit 1
      fi

      if [ -z "$SONAR_TOKEN" ]; then
        echo "❌ SONAR_TOKEN non défini"
        echo "📋 Configurez la variable CI/CD:"
        echo "   Nom: SONAR_TOKEN"
        echo "   Valeur: Votre token SonarQube"
        echo "   Pour générer un token:"
        echo "   1. Allez sur ${SONAR_HOST_URL}"
        echo "   2. My Account → Security → Generate Token"
        echo "   3. Nommez-le 'gitlab-ci'"
        echo "   4. Copiez et collez ici"
        exit 1
      fi

      echo "✅ Configuration SonarQube vérifiée"
      echo "🔗 URL: ${SONAR_HOST_URL}"
      echo "🔑 Token: ✅ Défini"

      # Vérification du rapport de couverture
      if [ ! -f "client/coverage/lcov.info" ]; then
        echo "⚠️  Fichier de couverture non trouvé: client/coverage/lcov.info"
        echo "📋 Génération d'un fichier de couverture minimal..."
        cd client
        mkdir -p coverage
        echo "TN:" > coverage/lcov.info
        echo "SF:src/App.js" >> coverage/lcov.info
        echo "FNF:0" >> coverage/lcov.info
        echo "FNH:0" >> coverage/lcov.info
        echo "LF:1" >> coverage/lcov.info
        echo "LH:0" >> coverage/lcov.info
        echo "BRF:0" >> coverage/lcov.info
        echo "BRH:0" >> coverage/lcov.info
        echo "end_of_record" >> coverage/lcov.info
        cd ..
      fi

      # Analyse frontend
      echo "🚀 Lancement de l'analyse SonarQube..."
      cd client

      # Création du fichier de configuration SonarQube
      cat > sonar-project.properties << EOF
      sonar.projectKey=ems-platform-frontend
      sonar.projectName=EMS Platform - Frontend
      sonar.projectVersion=${CI_COMMIT_SHORT_SHA}
      sonar.sources=src
      sonar.host.url=${SONAR_HOST_URL}
      sonar.login=${SONAR_TOKEN}
      sonar.javascript.lcov.reportPaths=coverage/lcov.info
      sonar.coverage.exclusions=**/*.test.js,**/*.spec.js,**/node_modules/**
      sonar.qualitygate.wait=false
      sonar.scm.provider=git
      sonar.sourceEncoding=UTF-8
      sonar.exclusions=**/node_modules/**,**/coverage/**,**/test-results/**
      EOF

      # Exécution de l'analyse
      sonar-scanner \
        -Dproject.settings=sonar-project.properties \
        -Dsonar.projectKey=ems-platform-frontend \
        -Dsonar.projectName="EMS Platform - Frontend" \
        -Dsonar.projectVersion=${CI_COMMIT_SHORT_SHA} \
        -Dsonar.sources=src \
        -Dsonar.host.url=${SONAR_HOST_URL} \
        -Dsonar.login=${SONAR_TOKEN} \
        -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info \
        -Dsonar.coverage.exclusions=**/*.test.js,**/*.spec.js,**/node_modules/** \
        -Dsonar.qualitygate.wait=false \
        -Dsonar.scm.provider=git

      echo "✅ Analyse SonarQube frontend terminée"
      echo "📈 Rapport disponible sur: ${SONAR_HOST_URL}/dashboard?id=ems-platform-frontend"
  artifacts:
    paths:
      - client/.scannerwork/
      - client/sonar-project.properties
    expire_in: 1 week
    when: always
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

sonarqube_backend:
  stage: analysis
  <<: *sonarqube_template
  dependencies:
    - install_backend_deps
    - test_backend
  script:
    - |
      echo "📊 Analyse SonarQube - Backend"

      # Vérification des variables obligatoires
      if [ -z "$SONAR_HOST_URL" ]; then
        echo "❌ SONAR_HOST_URL non défini"
        echo "📋 Configurez la variable CI/CD:"
        echo "   Nom: SONAR_HOST_URL"
        echo "   Valeur: http://localhost:9000"
        exit 1
      fi

      if [ -z "$SONAR_TOKEN" ]; then
        echo "❌ SONAR_TOKEN non défini"
        echo "📋 Configurez la variable CI/CD:"
        echo "   Nom: SONAR_TOKEN"
        echo "   Valeur: Votre token SonarQube"
        exit 1
      fi

      echo "✅ Configuration SonarQube vérifiée"
      echo "🔗 URL: ${SONAR_HOST_URL}"
      echo "🔑 Token: ✅ Défini"

      # Vérification du rapport de couverture
      if [ ! -f "backend/coverage/lcov.info" ]; then
        echo "⚠️  Fichier de couverture non trouvé: backend/coverage/lcov.info"
        echo "📋 Génération d'un fichier de couverture minimal..."
        cd backend
        mkdir -p coverage
        echo "TN:" > coverage/lcov.info
        echo "SF:index.js" >> coverage/lcov.info
        echo "FNF:0" >> coverage/lcov.info
        echo "FNH:0" >> coverage/lcov.info
        echo "LF:1" >> coverage/lcov.info
        echo "LH:0" >> coverage/lcov.info
        echo "BRF:0" >> coverage/lcov.info
        echo "BRH:0" >> coverage/lcov.info
        echo "end_of_record" >> coverage/lcov.info
        cd ..
      fi

      # Analyse backend
      echo "🚀 Lancement de l'analyse SonarQube..."
      cd backend

      # Création du fichier de configuration SonarQube
      cat > sonar-project.properties << EOF
      sonar.projectKey=ems-platform-backend
      sonar.projectName=EMS Platform - Backend
      sonar.projectVersion=${CI_COMMIT_SHORT_SHA}
      sonar.sources=.
      sonar.host.url=${SONAR_HOST_URL}
      sonar.login=${SONAR_TOKEN}
      sonar.javascript.lcov.reportPaths=coverage/lcov.info
      sonar.coverage.exclusions=**/*.test.js,**/*.spec.js,**/node_modules/**
      sonar.qualitygate.wait=false
      sonar.scm.provider=git
      sonar.sourceEncoding=UTF-8
      sonar.exclusions=**/node_modules/**,**/coverage/**,**/test-results/**
      EOF

      # Exécution de l'analyse
      sonar-scanner \
        -Dproject.settings=sonar-project.properties \
        -Dsonar.projectKey=ems-platform-backend \
        -Dsonar.projectName="EMS Platform - Backend" \
        -Dsonar.projectVersion=${CI_COMMIT_SHORT_SHA} \
        -Dsonar.sources=. \
        -Dsonar.host.url=${SONAR_HOST_URL} \
        -Dsonar.login=${SONAR_TOKEN} \
        -Dsonar.javascript.lcov.reportPaths=coverage/lcov.info \
        -Dsonar.coverage.exclusions=**/*.test.js,**/*.spec.js,**/node_modules/** \
        -Dsonar.qualitygate.wait=false \
        -Dsonar.scm.provider=git

      echo "✅ Analyse SonarQube backend terminée"
      echo "📈 Rapport disponible sur: ${SONAR_HOST_URL}/dashboard?id=ems-platform-backend"
  artifacts:
    paths:
      - backend/.scannerwork/
      - backend/sonar-project.properties
    expire_in: 1 week
    when: always
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

# === BUILD STAGE ===
build_frontend_image:
  stage: build
  <<: *docker_template
  dependencies:
    - test_frontend
    - sonarqube_frontend
  script:
    - |
      echo "🏗️ Building Frontend image..."
      docker build -t $FRONTEND_IMAGE:$COMMIT_TAG -t $FRONTEND_IMAGE:latest ./client
      echo "🔍 Built images:"
      docker images | grep "$FRONTEND_IMAGE"
      echo "✅ Frontend image built"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 20 minutes

build_backend_image:
  stage: build
  <<: *docker_template
  dependencies:
    - test_backend
    - sonarqube_backend
  script:
    - |
      echo "🏗️ Building Backend image..."
      docker build -t $BACKEND_IMAGE:$COMMIT_TAG -t $BACKEND_IMAGE:latest ./backend
      echo "🔍 Built images:"
      docker images | grep "$BACKEND_IMAGE"
      echo "✅ Backend image built"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 15 minutes

build_db_image:
  stage: build
  <<: *docker_template
  script:
    - |
      echo "🏗️ Building Database image..."
      docker build -t $DB_IMAGE:$COMMIT_TAG -t $DB_IMAGE:latest ./db
      echo "🔍 Built images:"
      docker images | grep "$DB_IMAGE"
      echo "✅ Database image built"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 10 minutes

# === IMAGE_SCAN STAGE ===
trivy_scan_frontend:
  stage: image_scan
  <<: *trivy_template
  dependencies:
    - build_frontend_image
  script:
    - |
      echo "🔍 Scanning Frontend image with Trivy..."
      mkdir -p artifacts
      trivy image --format json --output artifacts/trivy-frontend-scan.json $FRONTEND_IMAGE:$COMMIT_TAG || true
      trivy image --format table --output artifacts/trivy-frontend-scan.txt $FRONTEND_IMAGE:$COMMIT_TAG || true
      echo "✅ Frontend image scan completed"
  artifacts:
    paths:
      - artifacts/trivy-frontend-scan.json
      - artifacts/trivy-frontend-scan.txt
    expire_in: 1 week
    when: always
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

trivy_scan_backend:
  stage: image_scan
  <<: *trivy_template
  dependencies:
    - build_backend_image
  script:
    - |
      echo "🔍 Scanning Backend image with Trivy..."
      mkdir -p artifacts
      trivy image --format json --output artifacts/trivy-backend-scan.json $BACKEND_IMAGE:$COMMIT_TAG || true
      trivy image --format table --output artifacts/trivy-backend-scan.txt $BACKEND_IMAGE:$COMMIT_TAG || true
      echo "✅ Backend image scan completed"
  artifacts:
    paths:
      - artifacts/trivy-backend-scan.json
      - artifacts/trivy-backend-scan.txt
    expire_in: 1 week
    when: always
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

trivy_scan_db:
  stage: image_scan
  <<: *trivy_template
  dependencies:
    - build_db_image
  script:
    - |
      echo "🔍 Scanning Database image with Trivy..."
      mkdir -p artifacts
      trivy image --format json --output artifacts/trivy-db-scan.json $DB_IMAGE:$COMMIT_TAG || true
      trivy image --format table --output artifacts/trivy-db-scan.txt $DB_IMAGE:$COMMIT_TAG || true
      echo "✅ Database image scan completed"
  artifacts:
    paths:
      - artifacts/trivy-db-scan.json
      - artifacts/trivy-db-scan.txt
    expire_in: 1 week
    when: always
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true

# === INFRASTRUCTURE STAGE ===
infrastructure_terraform:
  stage: infrastructure
  image: hashicorp/terraform:latest
  script:
    - |
      echo "🏗️ Terraform Infrastructure Deployment"
      echo "📁 Checking if terraform directory exists..."
      if [ -d "terraform" ]; then
        cd terraform
        terraform init
        terraform validate
        terraform plan -out=plan.tfplan
        echo "✅ Infrastructure plan generated"
      else
        echo "⚠️ No terraform directory found, skipping..."
      fi
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
      when: manual
  tags:
    - windows
    - docker
    - ems-platform

infrastructure_kubernetes:
  stage: infrastructure
  image: bitnami/kubectl:latest
  script:
    - |
      echo "☸️ Kubernetes Manifests Validation"
      echo "📁 Checking if k8s directory exists..."
      if [ -d "k8s" ]; then
        kubectl apply --dry-run=client -f k8s/
        echo "✅ Kubernetes manifests validated"
      else
        echo "⚠️ No k8s directory found, skipping..."
      fi
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
  tags:
    - windows
    - docker
    - ems-platform

# === DEPLOY STAGE ===
deploy_staging:
  stage: deploy
  image: bitnami/kubectl:latest
  script:
    - |
      echo "🚀 Deploying to Staging Environment"
      echo "⚠️ Deployment configuration not implemented yet"
      echo "📋 To implement:"
      echo "   1. Configure kubectl context for staging"
      echo "   2. Apply Kubernetes manifests"
      echo "   3. Add rollout status checks"
      echo "📁 Current project structure:"
      ls -la
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev"'
      when: manual
  tags:
    - windows
    - docker
    - ems-platform

deploy_production:
  stage: deploy
  image: bitnami/kubectl:latest
  script:
    - |
      echo "🚀 Deploying to Production Environment"
      echo "⚠️ Deployment configuration not implemented yet"
      echo "📋 To implement:"
      echo "   1. Configure kubectl context for production"
      echo "   2. Apply Kubernetes manifests"
      echo "   3. Add rollout status checks"
      echo "📁 Current project structure:"
      ls -la
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
      when: manual
  tags:
    - windows
    - docker
    - ems-platform

# === FINALIZE STAGE ===
push_images:
  stage: finalize
  <<: *docker_template
  dependencies:
    - trivy_scan_frontend
    - trivy_scan_backend
    - trivy_scan_db
  script:
    - |
      echo "🚀 Pushing images to Docker Hub..."
      if [ "$CI_COMMIT_REF_NAME" = "main" ]; then
        echo "📦 Pushing Frontend image..."
        docker push $FRONTEND_IMAGE:$COMMIT_TAG
        docker push $FRONTEND_IMAGE:latest

        echo "📦 Pushing Backend image..."
        docker push $BACKEND_IMAGE:$COMMIT_TAG
        docker push $BACKEND_IMAGE:latest

        echo "📦 Pushing Database image..."
        docker push $DB_IMAGE:$COMMIT_TAG
        docker push $DB_IMAGE:latest

        echo "✅ All images pushed successfully"
      elif [ "$CI_COMMIT_REF_NAME" = "dev" ]; then
        echo "📦 Pushing dev images..."
        docker push $FRONTEND_IMAGE:$COMMIT_TAG
        docker push $BACKEND_IMAGE:$COMMIT_TAG
        docker push $DB_IMAGE:$COMMIT_TAG
        echo "✅ Dev images pushed successfully"
      fi
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 15 minutes

notify_success:
  stage: finalize
  image: alpine:latest
  script:
    - |
      echo "🎉 Pipeline completed successfully!"
      echo "📊 Summary:"
      echo "- Setup: ✅ Dependencies installed"
      echo "- Quality: ✅ ESLint & Prettier checks"
      echo "- Security: ✅ Audit & Trivy FS scan"
      echo "- Tests: ✅ Frontend & Backend tests"
      echo "- Analysis: ✅ SonarQube analysis"
      echo "- Build: ✅ 3 Docker images built"
      echo "- Image Scan: ✅ Trivy image scans"
      echo "- Infrastructure: ⚠️ Ready for implementation"
      echo "- Deploy: ⚠️ Ready for implementation"
      echo "- Finalize: ✅ Images pushed"

      if [ "$CI_COMMIT_REF_NAME" = "main" ]; then
        echo "🚀 Production images pushed to Docker Hub"
        echo "📦 Available images:"
        echo "  - $FRONTEND_IMAGE:latest"
        echo "  - $BACKEND_IMAGE:latest"
        echo "  - $DB_IMAGE:latest"
      elif [ "$CI_COMMIT_REF_NAME" = "dev" ]; then
        echo "🛠️ Development images tagged with commit SHA"
        echo "📦 Available images:"
        echo "  - $FRONTEND_IMAGE:$COMMIT_TAG"
        echo "  - $BACKEND_IMAGE:$COMMIT_TAG"
        echo "  - $DB_IMAGE:$COMMIT_TAG"
      fi

      echo ""
      echo "📈 SonarQube Reports:"
      if [ -n "$SONAR_HOST_URL" ] && [ -n "$SONAR_TOKEN" ]; then
        echo "  - Frontend: ${SONAR_HOST_URL}/dashboard?id=ems-platform-frontend"
        echo "  - Backend: ${SONAR_HOST_URL}/dashboard?id=ems-platform-backend"
      else
        echo "  ⚠️ SonarQube not configured"
      fi

      echo "⏱️ Pipeline completed at: $(date)"
  when: on_success
  tags:
    - windows
    - docker
    - ems-platform

cleanup:
  stage: finalize
  <<: *docker_template
  script:
    - |
      echo "🧹 Pipeline cleanup..."
      echo "🔍 Docker system info:"
      docker system df
      echo "🔄 Pruning unused containers, networks, and images..."
      docker system prune -f
      echo "✅ Cleanup completed"
  when: always
  allow_failure: true
  timeout: 5 minutes

workflow:
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_COMMIT_BRANCH == "dev"'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
