# .gitlab-ci.yml - OPTIMIZED FOR dev/main WORKFLOW
# dev = pre-production, main = production

stages:
  - dependencies
  - security
  - validate_test
  - build
  - deploy
  - clean

variables:
  DOCKER_TLS_CERTDIR: ""
  DOCKER_BUILDKIT: "1"
  NODE_VERSION: "20"
  POSTGRES_VERSION: "15"
  NEXT_TELEMETRY_DISABLED: "1"
  NPM_CONFIG_PREFER_OFFLINE: "true"
  NPM_CONFIG_AUDIT: "false"
  FRONTEND_IMAGE: "amineelalami/ems-platform-frontend"
  BACKEND_IMAGE: "amineelalami/ems-platform-backend"
  DB_IMAGE: "amineelalami/ems-platform-db"
  COMMIT_TAG: "sha-${CI_COMMIT_SHORT_SHA}"

cache:
  key: "npm-${CI_COMMIT_REF_SLUG}"
  paths:
    - client/node_modules/
    - backend/node_modules/
  policy: pull-push
  untracked: false

.docker_template: &docker_template
  image: docker:24
  services:
    - docker:24-dind
  variables:
    DOCKER_TLS_CERTDIR: ""
    DOCKER_BUILDKIT: "1"
  before_script:
    - docker info
    - echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
  tags:
    - windows
    - local

.node_template: &node_template
  image: node:${NODE_VERSION}
  before_script:
    - node --version
    - npm --version
  tags:
    - windows
    - local

install_frontend_deps:
  stage: dependencies
  <<: *node_template
  script:
    - Write-Host "📦 Installing Frontend dependencies..."
    - cd client
    - npm install --prefer-offline --no-audit
    - Write-Host "✅ Frontend dependencies installed"
  cache:
    key: "npm-${CI_COMMIT_REF_SLUG}"
    paths:
      - client/node_modules/
    policy: pull-push
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 10 minutes

install_backend_deps:
  stage: dependencies
  <<: *node_template
  script:
    - Write-Host "📦 Installing Backend dependencies..."
    - cd backend
    - npm install --prefer-offline --no-audit
    - npx prisma generate --no-hints
    - Write-Host "✅ Backend dependencies installed"
  cache:
    key: "npm-${CI_COMMIT_REF_SLUG}"
    paths:
      - backend/node_modules/
    policy: pull-push
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 10 minutes

audit_frontend:
  stage: security
  <<: *node_template
  script:
    - Write-Host "🔒 Frontend security audit..."
    - cd client
    - npm audit --audit-level high --production
    - Write-Host "✅ Frontend audit completed"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true
  timeout: 10 minutes

audit_backend:
  stage: security
  <<: *node_template
  script:
    - Write-Host "🔒 Backend security audit..."
    - cd backend
    - npm audit --audit-level high --production
    - Write-Host "✅ Backend audit completed"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true
  timeout: 10 minutes

validate_prisma_schema:
  stage: security
  <<: *node_template
  script:
    - Write-Host "🗄️ Prisma schema validation..."
    - cd backend
    - npx prisma validate
    - Write-Host "✅ Prisma schema validated"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 10 minutes

validate_frontend:
  stage: validate_test
  <<: *node_template
  script:
    - Write-Host "🔍 Frontend build validation..."
    - cd client
    - npm run build
    - Write-Host "✅ Frontend validated successfully"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 15 minutes

validate_backend:
  stage: validate_test
  <<: *node_template
  script:
    - Write-Host "🔍 Backend build validation..."
    - cd backend
    - npm run build
    - Write-Host "✅ Backend validated successfully"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 15 minutes

test_frontend:
  stage: validate_test
  <<: *node_template
  script:
    - Write-Host "🧪 Running Frontend tests..."
    - cd client
    - |
      if (Test-Path "package.json") {
        $testScript = npm run | Select-String "test"
        if ($testScript) {
          npm test -- --watchAll=false --passWithNoTests --silent
          Write-Host "✅ Frontend tests completed"
        } else {
          Write-Host "⚠️ No test script found in Frontend"
        }
      }
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true
  timeout: 15 minutes

test_backend:
  stage: validate_test
  <<: *node_template
  script:
    - Write-Host "🧪 Running Backend tests..."
    - cd backend
    - |
      if (Test-Path "package.json") {
        $hasJest = (Test-Path "jest.config.js") -or (Test-Path "jest.config.cjs") -or (Test-Path "jest.config.ts")
        if ($hasJest) {
          npm run test -- --silent --passWithNoTests
          Write-Host "✅ Backend tests completed"
        } else {
          Write-Host "⚠️ No Jest configuration found in Backend"
        }
      }
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true
  timeout: 15 minutes

build_frontend_image:
  stage: build
  <<: *docker_template
  script:
    - Write-Host "🏗️ Building Frontend image..."
    - docker build -t $FRONTEND_IMAGE:$COMMIT_TAG ./client
    - |
      if [[ "$CI_COMMIT_REF_NAME" == "main" ]]; then
        Write-Host "🚀 Pushing Frontend image to Docker Hub..."
        docker push $FRONTEND_IMAGE:$COMMIT_TAG
      else
        Write-Host "⏭️ Push skipped (dev branch - pre-production)"
      fi
    - Write-Host "✅ Frontend image built"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 15 minutes

build_backend_image:
  stage: build
  <<: *docker_template
  script:
    - Write-Host "🏗️ Building Backend image..."
    - docker build -t $BACKEND_IMAGE:$COMMIT_TAG ./backend
    - |
      if [[ "$CI_COMMIT_REF_NAME" == "main" ]]; then
        Write-Host "🚀 Pushing Backend image to Docker Hub..."
        docker push $BACKEND_IMAGE:$COMMIT_TAG
      else
        Write-Host "⏭️ Push skipped (dev branch - pre-production)"
      fi
    - Write-Host "✅ Backend image built"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 15 minutes

build_db_image:
  stage: build
  <<: *docker_template
  script:
    - Write-Host "🏗️ Building Database image..."
    - docker build -t $DB_IMAGE:$COMMIT_TAG ./db
    - |
      if [[ "$CI_COMMIT_REF_NAME" == "main" ]]; then
        Write-Host "🚀 Pushing Database image to Docker Hub..."
        docker push $DB_IMAGE:$COMMIT_TAG
      else
        Write-Host "⏭️ Push skipped (dev branch - pre-production)"
      fi
    - Write-Host "✅ Database image built"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 10 minutes

deploy_staging:
  stage: deploy
  image: alpine:latest
  environment:
    name: staging
    url: https://staging.ems-platform.com
  before_script:
    - apk add --no-cache openssh-client
  script:
    - Write-Host "🚀 AUTO Deploying to pre-production..."
    - |
      ssh -o StrictHostKeyChecking=no $STAGING_DEPLOY_USER@$STAGING_DEPLOY_SERVER "
        cd $STAGING_DEPLOY_PATH &&
        docker-compose pull --quiet &&
        docker-compose up -d --no-build &&
        echo '✅ Pre-production deployed successfully'"
    - Write-Host "✅ Pre-production deployment completed"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev"'
  tags:
    - windows
    - local
  timeout: 10 minutes

deploy_production:
  stage: deploy
  image: alpine:latest
  environment:
    name: production
    url: https://ems-platform.com
  before_script:
    - apk add --no-cache openssh-client curl
  script:
    - Write-Host "🚀 MANUAL Deploying to production..."
    - |
      ssh -o StrictHostKeyChecking=no $DEPLOY_USER@$DEPLOY_SERVER "
        cd $DEPLOY_PATH &&
        docker-compose pull --quiet &&
        docker-compose up -d --no-build &&
        echo '✅ Production deployed successfully'"
    - Write-Host "🔄 Health check..."
    - sleep 10
    - curl -f https://ems-platform.com/api/health || exit 1
    - Write-Host "✅ Production deployment completed"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
      when: manual
  tags:
    - windows
    - local
  timeout: 15 minutes

cleanup_docker:
  stage: clean
  image: docker:24
  services:
    - docker:24-dind
  variables:
    DOCKER_TLS_CERTDIR: ""
  script:
    - Write-Host "🧹 Docker cleanup..."
    - docker system prune -f
    - Write-Host "✅ Cleanup completed"
  tags:
    - windows
    - local
  when: always
  allow_failure: true
  timeout: 5 minutes

workflow:
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_COMMIT_BRANCH == "dev"'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'