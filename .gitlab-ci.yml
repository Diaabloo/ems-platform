# .gitlab-ci.yml - VERSION CORRIGÉE POUR VOTRE STRUCTURE
# Adapté pour client/ et backend/ (au lieu de frontend/ et backend/)

# =============================================
# CONFIGURATION GLOBALE
# =============================================

stages:
  - dependencies    # Installation des dépendances
  - security        # Audit sécurité et validation
  - validate_test   # Validation et tests
  - build           # Construction des images Docker
  - deploy          # Déploiement
  - clean           # Nettoyage

variables:
  # Configuration Docker
  DOCKER_TLS_CERTDIR: ""
  DOCKER_BUILDKIT: "1"  # BuildKit pour meilleures performances

  # Versions
  NODE_VERSION: "20"
  POSTGRES_VERSION: "15"

  # Optimisations
  NEXT_TELEMETRY_DISABLED: "1"
  NPM_CONFIG_PREFER_OFFLINE: "true"
  NPM_CONFIG_AUDIT: "false"

  # Noms des images Docker
  FRONTEND_IMAGE: "amineelalami/ems-platform-frontend"
  BACKEND_IMAGE: "amineelalami/ems-platform-backend"
  DB_IMAGE: "amineelalami/ems-platform-db"
  COMMIT_TAG: "sha-${CI_COMMIT_SHORT_SHA}"

# =============================================
# CACHE CONFIGURATION - NE JAMAIS SUPPRIMER node_modules
# =============================================

cache:
  key: "npm-${CI_COMMIT_REF_SLUG}"
  paths:
    - client/node_modules/      # CORRIGÉ : client/ au lieu de frontend/
    - backend/node_modules/
  policy: pull-push
  untracked: false
  when: on_success

# =============================================
# TEMPLATES RÉUTILISABLES
# =============================================

.docker_template: &docker_template
  image: docker:24
  services:
    - docker:24-dind
  variables:
    DOCKER_TLS_CERTDIR: ""
    DOCKER_BUILDKIT: "1"
  before_script:
    - docker info
    - echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
  tags:
    - windows
    - local

.node_template: &node_template
  image: node:${NODE_VERSION}
  before_script:
    - node --version
    - npm --version
  tags:
    - windows
    - local

# =============================================
# STAGE DEPENDENCIES - Installation séparée par service
# =============================================

install_frontend_deps:
  stage: dependencies
  <<: *node_template
  script:
    - Write-Host "📦 Installation des dépendances Frontend..."
    - cd client  # CORRIGÉ : client/ au lieu de frontend/
    - npm install --prefer-offline --no-audit
    - Write-Host "✅ Dépendances Frontend installées"
  cache:
    key: "npm-${CI_COMMIT_REF_SLUG}"
    paths:
      - client/node_modules/  # CORRIGÉ : client/ au lieu de frontend/
    policy: pull-push
  rules:
    - changes:
        - client/package.json        # CORRIGÉ
        - client/package-lock.json   # CORRIGÉ
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 10 minutes

install_backend_deps:
  stage: dependencies
  <<: *node_template
  script:
    - Write-Host "📦 Installation des dépendances Backend..."
    - cd backend
    - npm install --prefer-offline --no-audit
    - npx prisma generate --no-hints
    - Write-Host "✅ Dépendances Backend installées"
  cache:
    key: "npm-${CI_COMMIT_REF_SLUG}"
    paths:
      - backend/node_modules/
    policy: pull-push
  rules:
    - changes:
        - backend/package.json
        - backend/package-lock.json
        - backend/prisma/schema.prisma
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 10 minutes

# =============================================
# STAGE SECURITY - Audit et validation
# =============================================

audit_frontend:
  stage: security
  <<: *node_template
  script:
    - Write-Host "🔒 Audit de sécurité Frontend..."
    - cd client  # CORRIGÉ : client/ au lieu de frontend/
    - npm audit --audit-level high --production
    - Write-Host "✅ Audit Frontend complété"
  rules:
    - changes:
        - client/package.json  # CORRIGÉ
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true
  timeout: 5 minutes

audit_backend:
  stage: security
  <<: *node_template
  script:
    - Write-Host "🔒 Audit de sécurité Backend..."
    - cd backend
    - npm audit --audit-level high --production
    - Write-Host "✅ Audit Backend complété"
  rules:
    - changes:
        - backend/package.json
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true
  timeout: 5 minutes

validate_prisma_schema:
  stage: security
  <<: *node_template
  script:
    - Write-Host "🗄️ Validation du schéma Prisma..."
    - cd backend
    - npx prisma validate
    - Write-Host "✅ Schéma Prisma validé"
  rules:
    - changes:
        - backend/prisma/schema.prisma
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 3 minutes

# =============================================
# STAGE VALIDATE_TEST - Validation et tests parallèles
# =============================================

validate_frontend:
  stage: validate_test
  <<: *node_template
  script:
    - Write-Host "🔍 Validation du build Frontend..."
    - cd client  # CORRIGÉ : client/ au lieu de frontend/
    - npm run build
    - Write-Host "✅ Frontend validé avec succès"
  rules:
    - changes:
        - client/**  # CORRIGÉ
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 8 minutes

validate_backend:
  stage: validate_test
  <<: *node_template
  script:
    - Write-Host "🔍 Validation du build Backend..."
    - cd backend
    - npm run build
    - Write-Host "✅ Backend validé avec succès"
  rules:
    - changes:
        - backend/**
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  timeout: 6 minutes

test_frontend:
  stage: validate_test
  <<: *node_template
  script:
    - Write-Host "🧪 Exécution des tests Frontend..."
    - cd client  # CORRIGÉ : client/ au lieu de frontend/
    - |
      # Exécution conditionnelle des tests
      if (Test-Path "package.json") {
        $testScript = npm run | Select-String "test"
        if ($testScript) {
          npm test -- --watchAll=false --passWithNoTests --silent
          Write-Host "✅ Tests Frontend complétés"
        } else {
          Write-Host "⚠️ Aucun script de test trouvé dans Frontend"
        }
      }
  rules:
    - changes:
        - client/**  # CORRIGÉ
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true
  timeout: 8 minutes

test_backend:
  stage: validate_test
  <<: *node_template
  script:
    - Write-Host "🧪 Exécution des tests Backend..."
    - cd backend
    - |
      # Exécution conditionnelle des tests
      if (Test-Path "package.json") {
        $hasJest = (Test-Path "jest.config.js") -or (Test-Path "jest.config.cjs") -or (Test-Path "jest.config.ts")
        if ($hasJest) {
          npm run test -- --silent --passWithNoTests
          Write-Host "✅ Tests Backend complétés"
        } else {
          Write-Host "⚠️ Aucune configuration Jest trouvée dans Backend"
        }
      }
  rules:
    - changes:
        - backend/**
    - if: '$CI_COMMIT_REF_NAME == "dev" || $CI_COMMIT_REF_NAME == "main"'
  allow_failure: true
  timeout: 8 minutes

# =============================================
# STAGE BUILD - Construction des images Docker
# =============================================

build_frontend_image:
  stage: build
  <<: *docker_template
  script:
    - Write-Host "🏗️ Construction de l'image Frontend..."
    - docker build -t $FRONTEND_IMAGE:$COMMIT_TAG ./client  # CORRIGÉ : ./client
    - |
      # Push conditionnel - UNIQUEMENT sur main ou tags
      if [[ "$CI_COMMIT_REF_NAME" == "main" || "$CI_COMMIT_TAG" ]]; then
        Write-Host "🚀 Push de l'image Frontend..."
        docker push $FRONTEND_IMAGE:$COMMIT_TAG
      else
        Write-Host "⏭️ Push ignoré (branche: $CI_COMMIT_REF_NAME)"
      fi
    - Write-Host "✅ Image Frontend construite"
  rules:
    - changes:
        - client/**        # CORRIGÉ
        - client/Dockerfile*
    - if: '$CI_COMMIT_REF_NAME == "main"'
  timeout: 15 minutes

build_backend_image:
  stage: build
  <<: *docker_template
  script:
    - Write-Host "🏗️ Construction de l'image Backend..."
    - docker build -t $BACKEND_IMAGE:$COMMIT_TAG ./backend
    - |
      # Push conditionnel - UNIQUEMENT sur main ou tags
      if [[ "$CI_COMMIT_REF_NAME" == "main" || "$CI_COMMIT_TAG" ]]; then
        Write-Host "🚀 Push de l'image Backend..."
        docker push $BACKEND_IMAGE:$COMMIT_TAG
      else
        Write-Host "⏭️ Push ignoré (branche: $CI_COMMIT_REF_NAME)"
      fi
    - Write-Host "✅ Image Backend construite"
  rules:
    - changes:
        - backend/**
        - backend/Dockerfile*
    - if: '$CI_COMMIT_REF_NAME == "main"'
  timeout: 15 minutes

build_db_image:
  stage: build
  <<: *docker_template
  script:
    - Write-Host "🏗️ Construction de l'image Database..."
    - docker build -t $DB_IMAGE:$COMMIT_TAG ./db
    - |
      # Push conditionnel - UNIQUEMENT sur main ou tags
      if [[ "$CI_COMMIT_REF_NAME" == "main" || "$CI_COMMIT_TAG" ]]; then
        Write-Host "🚀 Push de l'image Database..."
        docker push $DB_IMAGE:$COMMIT_TAG
      else
        Write-Host "⏭️ Push ignoré (branche: $CI_COMMIT_REF_NAME)"
      fi
    - Write-Host "✅ Image Database construite"
  rules:
    - changes:
        - db/**
        - db/Dockerfile*
    - if: '$CI_COMMIT_REF_NAME == "main"'
  timeout: 10 minutes

# =============================================
# STAGE DEPLOY - Déploiement
# =============================================

deploy_staging:
  stage: deploy
  image: alpine:latest
  environment:
    name: staging
    url: https://staging.ems-platform.com
  before_script:
    - apk add --no-cache openssh-client
  script:
    - Write-Host "🚀 Déploiement en staging..."
    - |
      ssh -o StrictHostKeyChecking=no $STAGING_DEPLOY_USER@$STAGING_DEPLOY_SERVER "
        cd $STAGING_DEPLOY_PATH &&
        docker-compose pull --quiet &&
        docker-compose up -d --no-build &&
        echo '✅ Staging déployé avec succès'"
    - Write-Host "✅ Déploiement staging terminé"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "dev"'
  tags:
    - windows
    - local
  timeout: 10 minutes

deploy_production:
  stage: deploy
  image: alpine:latest
  environment:
    name: production
    url: https://ems-platform.com
  before_script:
    - apk add --no-cache openssh-client curl
  script:
    - Write-Host "🚀 Déploiement en production..."
    - |
      ssh -o StrictHostKeyChecking=no $DEPLOY_USER@$DEPLOY_SERVER "
        cd $DEPLOY_PATH &&
        docker-compose pull --quiet &&
        docker-compose up -d --no-build &&
        echo '✅ Production déployée avec succès'"
    - Write-Host "🔄 Vérification de santé..."
    - sleep 10
    - curl -f https://ems-platform.com/api/health || exit 1
    - Write-Host "✅ Déploiement production terminé"
  rules:
    - if: '$CI_COMMIT_REF_NAME == "main"'
      when: manual
  tags:
    - windows
    - local
  timeout: 15 minutes

# =============================================
# STAGE CLEAN - Nettoyage léger
# =============================================

cleanup_docker:
  stage: clean
  image: docker:24
  services:
    - docker:24-dind
  variables:
    DOCKER_TLS_CERTDIR: ""
  script:
    - Write-Host "🧹 Nettoyage Docker..."
    - docker system prune -f
    - Write-Host "✅ Nettoyage terminé"
  tags:
    - windows
    - local
  when: always
  allow_failure: true
  timeout: 5 minutes

# =============================================
# CONFIGURATION WORKFLOW
# =============================================

workflow:
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'
    - if: '$CI_COMMIT_BRANCH == "dev"'
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
    - if: '$CI_COMMIT_TAG'